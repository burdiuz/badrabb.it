import { ImageSetLocaleDirective } from './image-set-locale.directive';

describe('ImageSetLocaleDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSetLocaleDirective();
    expect(directive).toBeTruthy();
  });
});
