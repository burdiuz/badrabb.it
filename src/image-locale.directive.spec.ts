import { ImageLocaleDirective } from './image-locale.directive';

describe('ImageLocaleDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLocaleDirective();
    expect(directive).toBeTruthy();
  });
});
